#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 * print alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
char i, j;
for (i = 'a'; i <= 'z'; i++)
putchar(i);
for (j = 'A'; j <= 'Z'; i++)
putchar(j);
putchar('\n');
return (0);
}
