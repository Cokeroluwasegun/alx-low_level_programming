#include "main.h"
/**
 * print alphabet - Entry point
 * alphabet.c
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
int i;
for(i = 'a'; i <= 'z'; i++)
 {
  putchar(i);
 }
 putchar('\n');
}
